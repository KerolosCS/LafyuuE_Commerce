import 'package:flutter/material.dart';
import 'package:lafuu_e_commerce/screens/cart/pressentation/views/widgets/shape_to_body.dart';

class ShapeToView extends StatelessWidget {
  const ShapeToView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ShapeToViewBody(),
    );
  }
}
